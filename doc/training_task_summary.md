# Training Task Summary

This summary outlines the focus of each Groovy training task and the primary topics it reinforces from the Groovy Training deck.

| Task | Description | Topic Highlights |
| --- | --- | --- |
| 1 | Welcomes a new hire while printing property types to show dynamic typing and string interpolation. | Slides 2-4 (variables, typing) and 47 (CPI message access). |
| 2 | Formats badge text and message details using string helpers and interpolation for HR IDs. | Slides 3-5 (string operations) and 47 (message properties). |
| 3 | Calculates payroll adjustments with BigDecimal to demonstrate arithmetic and formatting. | Slides 4-5 (operators, numeric types) and 47 (payload handling). |
| 4 | Evaluates promotions with both ternary and full if/else chains for decision control. | Slides 6-7 (branching patterns) and 47 (properties). |
| 5 | Uses an HR training switch statement with ranges and fallbacks to classify progress. | Slide 6 (switch/range handling) and 47 (message updates). |
| 6 | Coordinates orientation rosters with for-each and while loops working together. | Slides 8-9 (loop constructs) and 47 (property usage). |
| 7 | Filters training sessions with Groovy collection helpers such as collect and findAll. | Slides 17-18 (list operations) and 47 (message access). |
| 8 | Summarizes attendance totals and averages from timesheets for HR reporting. | Slide 8 (loop totals) and slides 17-18 (list processing). |
| 9 | Validates raise requests with try/catch/finally to keep HR workflows resilient. | Slides 10-11 (exception handling) and 47 (message patterns). |
| 10 | Groups reusable methods with default parameters to summarize role and compensation updates. | Slides 12-13 (methods) and 47 (message helpers). |
| 11 | Parses score properties safely with try/catch to prevent bad numeric input. | Slides 10-11 (error handling) and 47 (message parsing). |
| 12 | Personalizes onboarding greetings through closures driven by message properties. | Slides 14-16 (closures) and 47 (payload access). |
| 13 | Spotlights HR teams by filtering employee lists with closure-based criteria. | Slides 14-16 (closures) and 17-20 (collections). |
| 14 | Manages onboarding course lists to practice Groovy list mutation helpers. | Slides 17-18 (list APIs) and 47 (message properties). |
| 15 | Adjusts department headcounts stored in maps for key-value collection practice. | Slides 19-20 (map operations) and 47 (message updates). |
| 16 | Builds a training schedule from numeric ranges for planning and iteration. | Slides 21-22 (ranges) and 47 (message handling). |
| 17 | Bundles employee data into a Task17EmployeeProfile class to explore objects. | Slides 23-24 (classes) and 47 (message access). |
| 18 | Applies raises and regex validation inside Task18PayrollEmployee to mix OOP and pattern checks. | Slides 24-25 (classes) and 44-45 (regex). |
| 19 | Demonstrates inheritance by extending Task19TeamMember into a team lead variant. | Slides 25-26 (inheritance) and 47 (message flows). |
| 20 | Contrasts abstract Task20StaffMember roles for interns and mentors in HR mentoring. | Slides 27-28 (abstract classes) and 47 (message handling). |
| 21 | Parses manager details from XML using XmlSlurper straight from the CPI message body. | Slides 29-31 (XmlSlurper) and 47 (payload access). |
| 22 | Compares XmlSlurper and XmlParser by reading identical manager XML with both APIs. | Slides 29-32 (XML parsing differences). |
| 23 | Builds a new employee directory XML using MarkupBuilder and message properties. | Slides 33-35 (XML generation) and 47 (message updates). |
| 24 | Removes inactive employee nodes from XML via XmlParser and XmlUtil serialization. | Slides 33-35 (XML manipulation) and 47 (payload handling). |
| 25 | Parses employee JSON to adjust salaries and skill counts in one pass. | Slides 36-40 (JSON parsing/transformation). |
| 26 | Logs CPI headers and creates an attachment to illustrate monitoring capabilities. | Slides 53-55 (CPI logging/attachments). |
| 27 | Supplies a reusable custom function for CPI mappings to format employee names. | Slides 56-57 (mapping functions). |
| 28 | Sorts HR employee data by multiple criteria with custom comparator logic. | Slides 42-43 (advanced sorting) and 47 (message access). |
| 29 | Retrieves value-mapping entries via the CPI API to enrich HR data. | Slides 56-57 (Integration Suite APIs). |
| 30 | Delays processing, then enriches an XML body with property-stored data. | Slides 53-57 (timers, XML enrichment, properties). |

