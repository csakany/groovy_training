import com.sap.gateway.ip.core.customdev.util.Message
import groovy.json.JsonSlurper
import groovy.json.JsonOutput

def Message processData(Message message) {
    /*
    Practice Task 25: Parse and modify JSON in one flow.
    - Load the employee JSON from a property and convert it with JsonSlurper.
    - Count the skills and adjust the salary just like the demonstration.
    - Format a summary for the body and keep both the counts and updated JSON as properties.
    Next step: Replace the TODO with your own JSON handling code.
    */
    // TODO: Implement the practice steps above.
    return message
}
