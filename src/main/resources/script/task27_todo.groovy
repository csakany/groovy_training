import com.sap.gateway.ip.core.customdev.util.Message

def Message processData(Message message) {
    /*
    Practice Task 27: Build a custom mapping function.
    - Create a helper method that combines title, first name, and last name into a display name.
    - Pull the same properties and use the helper within processData just like a CPI mapping would call it.
    - Place the result in the body and store it as a property.
    Next step: Replace the TODO with your own custom function.
    */
    // TODO: Implement the practice steps above.
    return message
}
