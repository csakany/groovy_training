package src.main.resources.script

import com.sap.gateway.ip.core.customdev.util.Message

/**
 * Task 5 practice script.
 *
 * Build the advanced Compound Employee consolidation demonstrated in task 5:
 * parse the XML, identify the latest job-information record for each person,
 * filter to Human Resources roles (or the keyword supplied via property), and
 * return a rich profile for each leader.
 */
def Message processData(Message message) {
    throw new UnsupportedOperationException("TODO: implement task 5 practice solution")
}
