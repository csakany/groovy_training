import com.sap.gateway.ip.core.customdev.util.Message

def Message processData(Message message) {
    /*
    Practice Task 5: Categorize onboarding progress with a switch.
    - Read a completion percentage from the message properties.
    - Use the same range-based switch style as the demo to map the percent to a helpful HR status.
    - Add the chosen status to the body and store it as a property for later steps.
    Next step: Replace the TODO with your own switch statement.
    */
    // TODO: Implement the practice steps above.
    return message
}
