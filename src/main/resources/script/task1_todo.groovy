package src.main.resources.script

import com.sap.gateway.ip.core.customdev.util.Message

/**
 * Task 1 practice script.
 *
 * Replace the exception with logic that mirrors the demonstration:
 * - Read onboarding details from message properties
 * - Build a single welcome string that mentions the colleague's name, job
 *   title, start date, and HR contact
 * - Store the result in the message body and add any helpful properties
 */
def Message processData(Message message) {
    throw new UnsupportedOperationException("TODO: implement task 1 practice solution")
}
