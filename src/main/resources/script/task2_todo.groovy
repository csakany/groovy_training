import com.sap.gateway.ip.core.customdev.util.Message

def Message processData(Message message) {
    /*
    Practice Task 2: Build an HR badge summary using string helpers.
    - Read an employee name, job title, and location from message properties.
    - Create a neat multi-line badge that shows each field plus initials computed from the name.
    - Store at least one derived value back to the properties (for example the badge label or initials).
    - Reuse the same functions demonstrated (split, collect, join, upper-case, interpolation).
    Next step: Follow the guidance above and replace the TODO with your own code.
    */
    // TODO: Implement the practice steps above.
    return message
}
