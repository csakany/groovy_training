import com.sap.gateway.ip.core.customdev.util.Message
import groovy.util.XmlParser
import groovy.xml.XmlUtil

def Message processData(Message message) {
    /*
    Practice Task 24: Remove XML nodes with XmlParser.
    - Parse the XML body with XmlParser.
    - Remove any Employee nodes that are not active, mirroring the demo logic.
    - Serialize the updated XML back to a string and store the number of removals as a property.
    Next step: Replace the TODO with your own removal logic.
    */
    // TODO: Implement the practice steps above.
    return message
}
