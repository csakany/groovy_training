import com.sap.gateway.ip.core.customdev.util.Message
import groovy.util.XmlSlurper

def Message processData(Message message) {
    /*
    Practice Task 24: Transform XML to CSV.
    1. Parse the XML document.
    2. Select a few FOCompany entries.
    3. Build a CSV string with a header row.
    4. Set the CSV content as the body.
    Next step: Remember to join the header and rows with new lines.
    */
    // TODO: Implement the practice steps above.
    return message
}
