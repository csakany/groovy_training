import com.sap.gateway.ip.core.customdev.util.Message

def Message processData(Message message) {
    /*
    Practice Task 18: Add behavior to a class.
    1. Create a class (for example PracticePayrollEmployee) with a salary field.
    2. Implement a method that increases the salary.
    3. Call the method and show the new salary.
    4. Save the updated value in a property.
    Next step: Update the salary field before returning from applyRaise.
    */
    // TODO: Implement the practice steps above.
    return message
}
