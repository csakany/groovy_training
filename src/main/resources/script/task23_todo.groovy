import com.sap.gateway.ip.core.customdev.util.Message
import groovy.xml.MarkupBuilder

def Message processData(Message message) {
    /*
    Practice Task 23: Generate XML with MarkupBuilder.
    - Read employee entries like "Name|Department" from a property.
    - Use MarkupBuilder to create an Employees XML document with child nodes for each entry.
    - Share the XML in the body and store the total number of employees as a property.
    Next step: Replace the TODO with your own MarkupBuilder code.
    */
    // TODO: Implement the practice steps above.
    return message
}
